<?php

use Illuminate\Foundation\Inspiring;
use Illuminate\Support\Facades\Artisan;
use Illuminate\Support\Facades\Log;

// Artisan::command('inspire', function () {
//     $this->comment(Inspiring::quote());
// })->purpose('Display an inspiring quote');

// // Register the RunQueue command
// Artisan::command('app:run-queue', function () {
//     Log::info('Running queue worker for cron');
//     $this->info('Running queue worker for cron');
//     Artisan::call('queue:work redis --sleep=3 --tries=3 --max-time=60');
//     $this->info('Queue worker completed');
// })->purpose('Run queue worker for cron execution');
